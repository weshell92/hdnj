
const nextConfig = {
  output: 'export',
  distDir: 'out',
  basePath: '/hdnj',
  images: {
    unoptimized: true,
  },
  reactStrictMode: true,
  poweredByHeader: false,
  experimental: {
    typedRoutes: false
  }
}

export default nextConfig
